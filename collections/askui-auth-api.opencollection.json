{
  "opencollection": "1.0.0",
  "info": {
    "name": "AskUI Workspaces access-tokens auth API",
    "version": "0.2.15"
  },
  "items": [
    {
      "info": {
        "name": "auth",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Send Verification Email",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "{{baseUrl}}/api/v1/auth/verification-email",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Creates a job to send a verification email (only for users that have not verified their email yet) whose status can be retrieved using the `GET /auth/jobs/{job_id}` endpoint"
        },
        {
          "info": {
            "name": "Get Job Status",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "{{baseUrl}}/api/v1/auth/jobs/:job_id",
            "params": [
              {
                "name": "job_id",
                "value": "",
                "type": "path"
              }
            ]
          },
          "docs": "Get Job Status"
        }
      ]
    }
  ],
  "bundled": true
}